{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "Run Debug Start",
      "command": "npm run start:dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}